# Nixpacks config for Railway
# Ensures Python and build tools are available for better-sqlite3's native addon

[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake", "nodejs_24"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/start.js"
